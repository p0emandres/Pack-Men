node_modules
dist
.DS_Store
*.log

# Rust/Anchor build artifacts
target/
**/target/

# =============================================================================
# CRITICAL: Wallet and Keypair Files (NEVER commit these)
# =============================================================================
# Solana wallet files
*-wallet.json
*wallet*.json
devnet-wallet.json
mainnet-wallet.json

# Anchor/Solana program keypairs
*-keypair.json
*keypair*.json

# Any JSON file that might contain keys (be explicit about what you DO want)
# If you need to commit specific JSON files, add them with !filename.json

# =============================================================================
# Environment variables (CRITICAL: Never commit secrets)
# =============================================================================
.env
.env.local
.env.*.local
.env.production
.env.development
server/.env
server/.env.local
server/.env.*.local

# BUT allow .env.example files (these contain no secrets, just templates)
!.env.example
!server/.env.example

# IDE and editor files
.vscode/
.idea/
*.swp
*.swo
*~

# OS files
Thumbs.db

# SSL certificates for local development
certs/
*.pem
*.key
.vercel
.env*.local
