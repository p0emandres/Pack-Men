# Force Railway to build the server, not the frontend
[phases.setup]
nixPkgs = ["nodejs_22"]

[phases.install]
cmds = ["cd server && npm ci"]

[phases.build]
cmds = ["cd server && npm run build"]

[start]
cmd = "cd server && npm start"
