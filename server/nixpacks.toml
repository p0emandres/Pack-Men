# Fix for Railway cache mount conflict with npm ci
[phases.install]
cmds = ["npm install --legacy-peer-deps"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "npm start"

# Disable the problematic cache
[options]
no-cache = true
