# =============================================================================
# Pack-Men Server Environment Variables
# =============================================================================
# Copy this file to .env and fill in your values

# Server Configuration
PORT=3001
NODE_ENV=development

# JWT Secret for session tokens (Required)
# Generate with: openssl rand -hex 32
JWT_SECRET=your_jwt_secret_here_generate_with_openssl_rand_hex_32

# Privy Authentication (Required)
# Get these from https://dashboard.privy.io
PRIVY_APP_ID=your_privy_app_id_here
PRIVY_APP_SECRET=your_privy_app_secret_here

# CORS Allowed Origins (comma-separated)
# Add your frontend domains
ALLOWED_ORIGINS=http://localhost:5173,http://localhost:3000,https://your-frontend.vercel.app

# =============================================================================
# Optional: Analytics & Monitoring
# =============================================================================

# Supabase (for analytics, optional)
# SUPABASE_URL=https://your-project.supabase.co
# SUPABASE_KEY=your_supabase_anon_key

# Peer Token Secret (falls back to JWT_SECRET if not set)
# PEER_TOKEN_SECRET=your_peer_token_secret
